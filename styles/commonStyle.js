import {colors} from "../utils/colors/colors"

export const commonStyles = {
    buttonStyle: {
        textTransform: 'none',
        background: colors.customLakeGreen,
        ":hover": {
            background: colors.customLakeGreen
        }
    }
}